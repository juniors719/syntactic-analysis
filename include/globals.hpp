#ifndef GLOBALS_HPP
#define GLOBALS_HPP

#include <map>
#include <vector>
#include <unordered_set>
#include <string>
#include "automata.hpp"

extern std::map<std::string, std::vector<std::vector<std::string>>> grammar;

#endif